<h1>New Post</h1>

<%= render 'form', post: @post %>

<div style="position:relative;top:30px;left:20px">
    <%= button_to 'Back', user_gallery_path(@gallery.user, @gallery), class: "btn btn-primary btn-lg", :method => :get %>
</div>
